inherit_from: .rubocop_todo.yml

inherit_gem:
  betterlint:
    - config/default.yml

inherit_mode:
  merge:
    - Exclude

AllCops:
  Exclude:
    - 'lib/generators/delayed_job/templates/*.rb'
  NewCops: enable

Rails/EnvironmentVariableAccess:
  Enabled: false
